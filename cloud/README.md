# Legacy Cloud Prototype

`cloud/` contains the early FastAPI cloud prototype for EspLink.

The active backend is now `../backend/`, which contains the Node/Express API, device WebSocket gateway, firmware upload/release flow, Prisma schema, and React admin frontend.

Keep this directory as historical reference until all useful notes have been migrated into `docs/`.
