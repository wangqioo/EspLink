# CLAUDE.md

Claude Code should use the same current backend guidance as Codex agents.

Read [AGENTS.md](./AGENTS.md) first. It contains the active monorepo paths, backend/admin/firmware/mini-program commands, current OTA validation status, and remaining hardware/deployment-gated work.

Do not use older Windows paths, `/Users/hushaohong/...`, or `/Users/wq/ai_deploy_backend` as current commands. Those paths only appear in historical records.
